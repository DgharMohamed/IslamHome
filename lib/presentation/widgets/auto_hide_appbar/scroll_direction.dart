/// Represents the direction of scrolling.
enum ScrollDirection {
  /// No scrolling is occurring.
  idle,

  /// Scrolling upward (content moving down).
  up,

  /// Scrolling downward (content moving up).
  down,
}
